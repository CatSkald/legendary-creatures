---
label: Зовнішні посилання
hide_body: true
fields:
- name: references
  type: field_group_list
  fields:
  - name: external_reference_type
    type: select
    default: Авторство зображення
    config:
      required: true
      options:
      - Авторство зображення
      - Wikipedia
      - IMDB
      - ISBN
      - YouTube
      - Інше
      source:
        type: simple
        section: 
        file: 
        path: 
    label: Тип посилання
  - name: external_reference_url
    type: text
    config:
      required: false
    label: Посилання як URL
  - name: reference
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: false
      schema:
        format: markdown
    label: Посилання як HTML
  config:
    min: 
    max: 
    labelField: external_reference_type
  label: Посилання
