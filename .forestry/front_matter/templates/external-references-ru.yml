---
label: Внешние ссылки
hide_body: true
fields:
- name: references
  type: field_group_list
  fields:
  - name: external_reference_type
    type: select
    default: Авторство изображения
    config:
      required: true
      options:
      - Авторство изображения
      - Wikipedia
      - IMDB
      - ISBN
      - YouTube
      - Другое
      source:
        type: simple
        section: 
        file: 
        path: 
    label: Тип ссылки
  - name: external_reference_url
    type: text
    config:
      required: false
    label: Ссылка как URL
  - name: reference
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: false
      schema:
        format: markdown
    label: Ссылка как HTML
  config:
    min: 
    max: 
    labelField: external_reference_type
  label: Ссылки
