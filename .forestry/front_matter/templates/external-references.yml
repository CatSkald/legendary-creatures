---
label: External References
hide_body: true
fields:
- name: references
  type: field_group_list
  fields:
  - name: external_reference_type
    type: select
    default: Image attribution
    config:
      required: true
      options:
      - Image attribution
      - Wikipedia
      - IMDB
      - ISBN
      - YouTube
      - Other
      source:
        type: simple
        section: 
        file: 
        path: 
    label: Type
  - name: external_reference_url
    type: text
    config:
      required: false
    label: Reference as URL
  - name: reference
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: false
      schema:
        format: markdown
    label: Reference as HTML
  config:
    min: 
    max: 
    labelField: external_reference_type
  label: References
